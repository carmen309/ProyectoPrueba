@model Opositae.Models.Legislacion
@{
    ViewBag.Title = "Borrar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Inicio</a></li>
    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Legislacion")">Legislación</a></li>
    <li class="breadcrumb-item active">Borrar</li>
</ol>
<div class="row">
    <div class="text-right margin-b-10">
        <button type="button" class="btn btn-info" onclick="location.href='@Url.Action("Index", "Legislacion")'">
            <i class="material-icons btn__icon--left">arrow_back</i> Volver
        </button>
    </div>
    <div class="card">
        <div class="card-block">
            <h5>Borrar norma</h5>
            <hr />
            <div class="alert alert-warning" role="alert">
                <strong>¡Atención!</strong> El borrado de este elemento será irreversible.
            </div>
            <dl class="dl-horizontal">
                <dt>
                    Categoría
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Categoria.Nombre)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Norma)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Norma)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Enlace)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Enlace)
                </dd>
            </dl>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="text-right">
                    <button type="submit" class="btn btn-danger">
                        <i class="material-icons btn__icon--left">delete</i> Borrar
                    </button>
                </div>

            }

        </div>
    </div>
</div>